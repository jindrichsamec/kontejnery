general:
  build_dir: frontend

machine:
  node:
    version: 6.5.0

deployment:
  prod:
    branch: master
    commands:
      - npm run build
      - aws s3 sync ./build s3://www.velkoobjemaky.cz/ --delete --region=eu-central-1 --acl=public-read
