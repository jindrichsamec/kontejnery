import React from 'react';
import ReactDOM from 'react-dom';
import dotenv from 'dotenv'
import App from './App';
import './index.css';

dotenv.config()

ReactDOM.render(
  <App />,
  document.body
);
